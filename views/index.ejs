<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Exidents Happen</title>
    <link rel="stylesheet" type="text/css" href="exidentstatic/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
          <% if (breakups !== null && lastRecordedDate !== null) { %>
            <p><%= breakups %> tweets about breakups have been made since <%= lastRecordedDate %></p>
          <% } %>
          <input type="button" onClick="history.go(0)" class="ghost-button" value="Refresh Sadness">
      </fieldset>
    </div>
    <div class="chart-area">
	    <canvas id="charty"></canvas>
    </div>
  	<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.5.0/Chart.js"></script>
    <script type="text/javascript">
		  var ctx = document.getElementById('charty').getContext('2d'),
	  	chart = new Chart(ctx, {
			type: 'line',
			data: {
				labels: <%- JSON.stringify(dateRange) %>,
				datasets: [{
					label: 'Breakups',
					borderColor: '#ff0000',
          data: <%- JSON.stringify(breakupData) %>,
          backgroundColor: 'rgba(255,99,132,0.2)' }]
      },
        options: {
	      title: {
		    display: true,
		    text: 'Daily tweets about breakups'
	      },
         fill: false,
         scales: {
           yAxes: [{
             gridLines: false
           }]
         }
        }
      });
    </script>
      <div>
        We're not responsible for relationship problems as a result of using this website. Correlation is not causation!
      </div>
  </body>

</html>
